# Assets

The engine can run without external art by using rectangle fallbacks.

To enable the animated player sprite, add `player.bmp` to this folder. The current sprite setup expects a horizontal 4-frame sheet with four 32x32 frames, for a total image size of 128x32 pixels.
